package ${package}.modules.${module}.dao;

import org.apache.ibatis.annotations.Mapper;

import ${package}.modules.${module}.entity.${className}Entity;
import net.chenlin.dp.common.mapper.TkBaseMapper;

/**
 * ${comments}
 * @author ${author}<${email}>
 */
@Mapper
public interface ${className}Mapper extends TkBaseMapper<${className}Entity> {
	
}
